# ── Core API ──────────────────────────────────────────────────────────────────
fastapi
uvicorn[standard]
python-multipart
httpx

# ── Data ──────────────────────────────────────────────────────────────────────
pandas
numpy<2.0.0
scikit-learn
scipy

# ── ML Models ─────────────────────────────────────────────────────────────────
xgboost
lightgbm
prophet
statsmodels
joblib

# ── MLflow ────────────────────────────────────────────────────────────────────
# ⚠️  DO NOT UPGRADE without migrating mlruns metadata first.
#     MLflow >= 2.x requires a `run_uuid` field in every run's meta.yaml.
#     Runs logged with older MLflow versions lack this field and will cause:
#       TypeError: RunInfo.__init__() missing 1 required positional argument: 'run_uuid'
#     To upgrade: add `run_uuid: <run_id>` to all mlruns/**/meta.yaml files
#     (excluding mlruns/**/models/**/meta.yaml) before bumping this version.
mlflow==2.10.2

# ── Prefect ───────────────────────────────────────────────────────────────────
prefect

# ── Testing ───────────────────────────────────────────────────────────────────
pytest
pytest-asyncio
pytest-timeout
deepchecks

# ── Code Quality ──────────────────────────────────────────────────────────────
ruff
fakeredis<2.35.0



# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv
pydantic
pydantic-settings
requests
aiofiles
